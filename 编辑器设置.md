# 编辑器初始化（一次性，约 2 分钟）

代码已经全部写好，但 Cocos Creator 的场景文件 (.scene) 必须在编辑器里手动创建。按下面步骤做一次即可。

## 步骤

### 1. 打开项目
- 启动 Cocos Creator 3.8.x
- 打开本仓库根目录（`MyGrandmaGames/`）
- 编辑器会自动扫描 `assets/scripts/` 下所有 `.ts`，编译完成后才能用

### 2. 创建主菜单场景

1. 在 **Assets** 面板里，进入 `assets/scenes/` 目录（如果不存在就在 `assets/` 下右键 → Create → Folder → 命名 `scenes`）
2. 在 `scenes/` 里右键 → **Create → Scene** → 命名 `MainScene`
3. 双击打开 `MainScene`
4. 在 **Hierarchy** 面板右键 → **Create Empty Node** → 命名 `Bootstrap`
5. 选中 `Bootstrap` 节点，在 **Inspector** 面板点 **Add Component** → 搜索 `MainMenu` → 添加
6. **Ctrl+S** 保存

> 注意：默认场景里可能已经有 `Main Camera` / `Canvas` 节点，**全部删掉**。我们的 Bootstrap 组件会自己创建 Camera 和 Canvas。

### 3. 创建泡泡龙场景

1. 在 `scenes/` 里右键 → **Create → Scene** → 命名 `BubbleShooter`
2. 双击打开 `BubbleShooter`
3. 删掉默认的 Camera/Canvas 节点
4. 创建 `Bootstrap` 空节点
5. 给 `Bootstrap` 加 `BubbleGame` 组件
6. 保存

### 4. 设置启动场景

- **项目** → **项目设置** → **项目数据** → **启动场景**：选 `MainScene`
- 或在 Assets 里右键 `MainScene` → **Set as Start Scene**

### 5. 预览

- 顶部菜单 → **预览** → **浏览器预览** （或按 Ctrl+P）
- 浏览器应打开 `http://localhost:7456/`
- 看到："怀旧小游戏" 标题、"金币: 0"、"开始泡泡龙" 按钮、"清空存档" 按钮
- 点 "开始泡泡龙" → 跳转到泡泡龙场景，看到顶部 5 行彩色泡泡 + 底部发射器
- 鼠标在屏幕上移动 → 从发射器出发的白色虚线（碰墙反弹）跟着鼠标方向变化
- 点击 → 一个泡泡沿虚线方向飞出，碰到屏幕边界反弹，飞出顶部消失（Day 6 之后改成吸附）
- 左上 "返回" → 回主菜单

## 当前已实现（Day 2-5）

- ✅ 核心层：SaveManager（本地存档）、GameManager（奖励入口）、EventBus、MiniGameBase、AudioManager（stub）
- ✅ SaveData 结构（已预留养成字段）
- ✅ 主菜单：标题 + 金币显示 + 开始按钮 + 清空存档按钮
- ✅ 泡泡龙：六边形网格 + 5 色泡泡随机填充 + 发射器 + 瞄准虚线（带反弹）+ 发射飞行 + 边界反弹

## 还没做（Day 6+）

- ❌ 飞行泡泡碰到现有泡泡时的吸附
- ❌ 同色 3+ 消除（Grid.getNeighbors 已写好备用）
- ❌ 悬空泡泡掉落
- ❌ 无尽模式：定时下推一行
- ❌ 胜负判定（泡泡触底 = 输；清空 = 赢）
- ❌ 结算面板 + 奖励金币入账
- ❌ 真实美术、音效

## 编辑器操作问题排查

- **找不到 Add Component 里的脚本**：Cocos 编辑器没编译成功，看底部日志面板有没有红色 TS 错误
- **预览空白**：检查启动场景是不是设了 MainScene
- **金币显示不刷新**：检查浏览器 console 有没有 EventBus 报错
- **触摸/鼠标无反应**：确认场景里只有一个 Camera（我们脚本创建的那个），删掉默认 Camera
