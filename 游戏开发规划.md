# 怀旧小游戏合集 + 养成系统 - 项目规划

> 创建于 2026-05-14，作为开发参考文档

---

## 一、项目概述

**产品定位**：微信平台上的怀旧小游戏合集，外层套养成系统（类似《暴走小镇》《动物餐厅》的玩法逻辑）。

**核心玩法循环**：
1. 玩怀旧小游戏 → 获得金币/经验
2. 用金币养成宠物/角色 / 解锁新内容
3. 养成解锁更多小游戏或加成 → 反哺游戏体验

**首发小游戏**：泡泡龙（无尽模式）

---

## 二、技术选型

| 项目 | 选择 | 理由 |
|------|------|------|
| 引擎 | **Cocos Creator 3.x** | 国内生态最完善，对微信小游戏官方适配最好，支持多场景管理 |
| 语言 | TypeScript | Cocos 默认推荐，类型安全 |
| 平台 | 微信小程序（开发期）→ 微信小游戏（商业化期）| 体验版无审核，适合家人测试 |
| 后端 | **微信云开发（CloudBase）** | 个人开发者可用，免费额度足够，与微信深度集成 |
| 存档 | 本地 storage + 云开发数据库 | 双存档，离线可玩 + 跨设备同步 |
| 美术 | AI 生成（Midjourney / 即梦 / Leonardo.ai 等） | 个人开发者最低成本方案 |

---

## 三、发布路线

### 阶段 1：体验版（无审核，家人玩）
- 微信开发者工具上传体验版
- 添加家人为体验成员（最多 90 人）
- **当前阶段** ✅

### 阶段 2：正式发布
- 注册个体工商户（成本几百元，1 周）
- 申请微信小游戏主体资质
- 提交审核 → 上架

### 阶段 3：商业化
- 接入广告（激励视频、Banner）
- 内购（金币包、解锁宠物等）
- 需要游戏版号（如果走付费路线）

---

## 四、项目目录结构（养成-ready 架构）

```
assets/
├── scenes/
│   ├── MainScene.scene         # 主菜单（未来扩展为养成主界面）
│   ├── BubbleShooter.scene     # 泡泡龙
│   └── _MiniGameTemplate.scene # 模板，新游戏从这复制
├── scripts/
│   ├── core/                   # ⭐ 核心层（先搭好，永不变）
│   │   ├── GameManager.ts      # 全局单例：当前玩家、运行时状态
│   │   ├── SaveManager.ts      # 存档读写（本地 + 云）
│   │   ├── CloudSync.ts        # 云开发同步逻辑
│   │   ├── EventBus.ts         # 跨场景事件
│   │   ├── AudioManager.ts     # 音效/BGM
│   │   └── MiniGameBase.ts     # ⭐ 小游戏基类，定义契约
│   ├── data/
│   │   ├── SaveData.ts         # ⭐ 存档结构（预留养成字段）
│   │   └── Config.ts           # 全局配置常量
│   ├── minigames/
│   │   └── bubble/             # 泡泡龙
│   │       ├── BubbleGame.ts   # 主控（继承 MiniGameBase）
│   │       ├── Grid.ts         # 六边形网格管理
│   │       ├── Bubble.ts       # 单个泡泡
│   │       ├── Shooter.ts      # 发射器
│   │       └── EndlessMode.ts  # 无尽模式逻辑（顶部下推）
│   └── ui/
│       ├── MainMenu.ts         # 主菜单 UI
│       └── GameOverPanel.ts    # 通用结算面板
└── resources/                  # 动态加载的资源
```

---

## 五、关键架构设计（确保养成系统能无缝接入）

### 5.1 存档结构（SaveData.ts）

**最重要的一步**：一开始就预留养成字段，未来加养成不用改结构。

```typescript
export interface SaveData {
    version: number;            // 存档版本号

    player: {
        nickname: string;
        openId?: string;         // 微信 openId（云存档需要）
        createdAt: number;
    };

    // 通用资源
    currency: {
        coins: number;          // 金币
        gems: number;           // 钻石（高级货币，预留）
    };

    // 各小游戏进度
    minigames: {
        [gameId: string]: {     // 'bubble' | 'snake' | ...
            highScore: number;
            unlockedLevels: number;
            playCount: number;
            lastPlayedAt: number;
        };
    };

    // ⭐ 养成系统（现在为空，未来填充）
    pet: {
        [key: string]: any;
    };
    inventory: {
        items: { [itemId: string]: number };
    };
    achievements: string[];

    // 设置
    settings: {
        bgmVolume: number;
        sfxVolume: number;
    };

    // 云同步元数据
    sync: {
        lastSyncAt: number;
        dirty: boolean;          // 本地有未同步修改
    };
}
```

### 5.2 小游戏统一契约（MiniGameBase.ts）

```typescript
export abstract class MiniGameBase extends Component {
    // 入场参数（养成系统未来传"宠物加成"等）
    protected gameContext: {
        difficulty?: number;
        bonuses?: Record<string, number>;
    } = {};

    abstract get gameId(): string;
    abstract startGame(): void;

    protected endGame(result: MiniGameResult) {
        SaveManager.recordGameResult(this.gameId, result);
        GameManager.handleReward(this.gameId, result);
        EventBus.emit('minigame.ended', result);
    }
}

export interface MiniGameResult {
    success: boolean;
    score: number;
    duration: number;
    extra?: Record<string, any>;
}
```

**未来接养成系统**：只改 `GameManager.handleReward`（决定加什么奖励）和 `gameContext.bonuses`（传入加成），不动具体游戏代码。

### 5.3 场景切换策略

**用最简单的：场景全切换**
- 主菜单点泡泡龙 → `director.loadScene('BubbleShooter')`
- 游戏结束 → `director.loadScene('MainScene')`
- 数据通过单例 `GameManager` 跨场景传递

未来要做"小游戏中宠物在角落出现"等高级交互再升级到"常驻层 + Prefab 动态加载"，**当前不影响**。

### 5.4 云存档策略（微信云开发）

**双存档机制**：
1. **本地优先**：所有读写先操作本地 storage（即时响应、离线可玩）
2. **云端同步**：关键时机触发云同步
   - 进入游戏时：拉取云存档与本地比较，取较新
   - 游戏结束时：写入云端
   - 退出小程序时：最后同步一次

**冲突处理**：
- 用 `version` 字段判断
- 多端同时玩按"最后写入获胜"
- 关键资源（金币）取较大值（避免家人之间互相覆盖）

**云开发结构**：
```
集合（数据库表）：
├── users           # 玩家档案
├── saves           # 存档主表（按 openId 索引）
└── leaderboards    # 排行榜（家人间比分用）
```

---

## 六、第一阶段：泡泡龙无尽模式

### 6.1 玩法规则
- 顶部以六边形交错网格排列泡泡
- 玩家在底部瞄准发射
- 同色 3+ 连接 → 消除
- 悬空泡泡掉落（计算奖励）
- **无尽模式**：每隔 N 秒或 N 次发射后，整体下推一行
- 失败：泡泡触底
- 计分：消除数 × 难度系数 + 连击加成

### 6.2 关键技术点

| 模块 | 实现思路 |
|------|---------|
| 六边形网格 | 奇偶行错位，存储用二维数组 + 行偏移 |
| 瞄准线预测 | 射线投射 + 边界反弹模拟 |
| 碰撞吸附 | 检测射线第一次接触的网格位置 |
| 同色消除 | BFS 遍历相邻同色泡泡 |
| 悬空检测 | 从顶行 BFS，不可达的全部掉落 |
| 顶部下推 | 整体网格 Y 坐标平移 + 新增一行 |

### 6.3 第一周开发清单

| 天数 | 任务 | 验收标准 |
|------|------|---------|
| Day 1 | 装 Cocos Creator 3.x、微信开发者工具；新建项目 | "Hello World" 在微信开发者工具里能跑 |
| Day 2 | 搭核心层：SaveManager、GameManager、EventBus、SaveData | 存档读写跑通，能持久化金币 |
| Day 3 | MainScene 简陋版：一个按钮"开始泡泡龙"；显示金币 | 点击按钮能切换场景 |
| Day 4-5 | 泡泡龙：网格搭建、泡泡显示、发射器、瞄准线 | 能瞄准、看到预测线、发射 |
| Day 6 | 泡泡龙：碰撞吸附、同色消除、悬空掉落 | 完整消除逻辑跑通 |
| Day 7 | 无尽模式下推、胜负判定、结算面板、奖励金币写入存档；上传体验版 | 家人能扫码玩 |

---

## 七、待办（之后的阶段）

### 即将做（第 2-4 周）
- [ ] 接入微信云开发，做云存档
- [ ] 美术资源替换（AI 生成的泡泡、背景）
- [ ] 音效、BGM
- [ ] 更多怀旧小游戏：贪吃蛇 / 2048 / 打地鼠

### 后续做（第 2 个月+）
- [ ] 养成系统 v1：单一宠物 + 饱食度
- [ ] 道具系统：玩游戏掉落道具，喂养宠物
- [ ] 装扮系统
- [ ] 成就系统
- [ ] 排行榜（家人间比分）

### 商业化阶段
- [ ] 注册个体工商户
- [ ] 切换到微信小游戏构建模板
- [ ] 接入广告 SDK（激励视频回血）
- [ ] 内购（解锁高级宠物）

---

## 八、参考资源

- [Cocos Creator 官方文档](https://docs.cocos.com/creator/manual/zh/)
- [微信小游戏开发文档](https://developers.weixin.qq.com/minigame/dev/guide/)
- [微信云开发文档](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/getting-started.html)
- 美术素材备选：itch.io / OpenGameArt.org / Kenney.nl（CC0 免费）
- AI 美术工具：Midjourney / 即梦（字节）/ 通义万相（阿里）/ Leonardo.ai（游戏向）/ Scenario.gg（专门做游戏资产）

---

## 九、开发环境（双机模式）

**主力开发机**（不装微信开发者工具）：
- Cocos Creator 3.8.x LTS
- VSCode
- Node.js LTS
- Git

**测试/构建/上传机**：
- Cocos Creator 3.8.x（**版本必须与主力机完全一致**）
- 微信开发者工具
- Git

**工作流**：
- 主力机日常开发，用 Cocos **浏览器预览**快速迭代
- 通过 Git 同步到测试机
- 测试机做小程序构建 → 微信开发者工具上传体验版给家人

**已知限制**：
- 微信特有 API（云开发、登录、支付、分享）浏览器预览跑不通，需在测试机调
- 用 `if (CC_WECHATGAME)` 包裹微信特有代码，集中调试时切测试机

---

## 十、关键决策记录

| 日期 | 决策 | 理由 |
|------|------|------|
| 2026-05-14 | 用 Cocos Creator 而非 Phaser | 多场景管理 + 后续小游戏迁移方便 |
| 2026-05-14 | 走微信小程序开发，未来转小游戏 | 个人主体限制，体验版无审核可立即测试 |
| 2026-05-14 | 首发选泡泡龙无尽模式 | 逻辑成熟，无尽模式适合无关卡设计简单起步 |
| 2026-05-14 | 用微信云开发做云存档 | 个人可用，免费额度，与微信深度集成 |
| 2026-05-14 | 美术 AI 生成 | 个人开发者无美术，最低成本 |
| 2026-05-14 | 双机开发：主力机不装微信开发者工具 | 用户偏好；用浏览器预览 + 测试机构建上传 |
